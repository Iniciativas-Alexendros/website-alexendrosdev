# Sprint 1 — Issues #78-#82

**Fecha:** 2026-07-17
**Estado:** planificado
**Branch base:** `main`

---

## Estado de issues

| #   | Título                                               | Estado previo                                                                 | Depende de                       | Prioridad       |
| --- | ---------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------- | --------------- |
| #78 | Fusionar Escaparate en Servicios + redefinir pricing | ⚠️ Escaparate ya redirige a `/servicios`                                      | —                                | **P1**          |
| #79 | Reescribir README orientado a cliente                | Pendiente                                                                     | #78, #80 (estado final del repo) | P3              |
| #80 | Eliminar Blog + backup Notion                        | Pendiente                                                                     | Plan externo de migración Notion | P3              |
| #81 | Limpiar raíz — .claude, .audit-shots, defectos.*     | ✅ **Parcial** (PR #83 mergeado a main; .claude integrado pendiente de merge) | Merge de la rama freebuff        | Hecho (parcial) |
| #82 | Activar semantic-release en workflow de main         | ⚠️ release.yml ya existe, falta verificar                                     | —                                | **P2**          |

---

## Análisis de dependencias

```
#81 (limpiar raíz) ──► PARCIAL ✅ (falta mergear .claude/ → main)
  │
  ├── #82 (semantic-release)     ← independiente, puede empezar ahora
  ├── #78 (fusionar Escaparate)  ← independiente, escaparate ya redirige
  ├── #80 (eliminar Blog)        ← requiere backup Notion externo
  └── #79 (README orientado)     ← mejor después de #78 y #80
```

---

## Sprint 1 (ahora)

### 1.1 #78 — Fusionar Escaparate en Servicios + redefinir pricing

**Rama:** `feat/merge-escaparate-servicios`

**Estado actual:** `/escaparate` ya es un redirect puro a `/servicios`:

```tsx
// src/app/escaparate/page.tsx
redirect("/servicios");
```

**Tareas:**

| #    | Tarea                                                                  | Archivos                                                    | Estimación |
| ---- | ---------------------------------------------------------------------- | ----------------------------------------------------------- | ---------- |
| 78.1 | Eliminar página `/escaparate` (redirect sobrante)                      | `src/app/escaparate/page.tsx`, `src/app/escaparate/`        | 5 min      |
| 78.2 | Verificar que ningún nav/enlace apunte a `/escaparate`                 | `src/components/sections/Header.tsx`, sitemap               | 5 min      |
| 78.3 | Revisar `services.ts`: ¿tiers de proyecto coherentes con catalog.ts?   | `src/lib/content/services.ts`, `src/lib/content/catalog.ts` | 15 min     |
| 78.4 | Unificar nomenclatura: todo `services` vs `servicios` según convención | `src/lib/content/services.ts`                               | 10 min     |
| 78.5 | Verificar que el checkout/Success URLs no referencien `/escaparate`    | `src/app/api/checkout/route.ts`                             | 5 min      |

**Total estimado:** ~40 min

### 1.2 #82 — Verificar y activar semantic-release

**Rama:** `chore/verify-semantic-release`

**Estado actual:** `.releaserc.json` ✅ + `.github/workflows/release.yml` ✅

**Tareas:**

| #    | Tarea                                                                      | Archivos                        | Estimación |
| ---- | -------------------------------------------------------------------------- | ------------------------------- | ---------- |
| 82.1 | Verificar que `release.yml` tiene `GITHUB_TOKEN` con permisos de escritura | `.github/workflows/release.yml` | 5 min      |
| 82.2 | Verificar que los `branches` en `.releaserc.json` coinciden con main       | `.releaserc.json`               | 2 min      |
| 82.3 | Añadir `permissions: { contents: write }` si falta en release.yml          | `.github/workflows/release.yml` | 2 min      |
| 82.4 | Hacer un dry-run local (si es posible) o push simulado                     | —                               | 10 min     |

**Total estimado:** ~20 min

---

## Sprint 2 (futuro)

### 2.1 #80 — Eliminar Blog + backup Notion

**Dependencia:** Tener backup Notion del contenido antes de eliminar.

**Tareas:** Migrar 7 MDX a Notion → eliminar `content/blog/` → eliminar `src/app/blog/` → limpiar `sitemap.ts` y `feed.xml`.

### 2.2 #79 — Reescribir README orientado a cliente

**Dependencia:** Estado final del repo tras #78 y #80.

**Tareas:** Redactar README comercial → mover ARCHITECTURE.md, ROADMAP.md, AGENTS.md a `docs/` → actualizar enlaces.

---

## Resumen de ramas

| Rama                              | Issue | Base   | Estado       |
| --------------------------------- | ----- | ------ | ------------ |
| `feat/merge-escaparate-servicios` | #78   | `main` | 🆕 Por crear |
| `chore/verify-semantic-release`   | #82   | `main` | 🆕 Por crear |

---

## Criterios de aceptación del Sprint 1

- [ ] `src/app/escaparate/` eliminado sin referencias rotas
- [ ] `catalog.ts` y `services.ts` coherentes (pricing tiers)
- [ ] `release.yml` verificado y funcional
- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm build` en verde
